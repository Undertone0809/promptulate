# React Agent Example

Run from the repository root:

```bash
export OPENAI_API_KEY=your_key
uv run python use_cases/react_agent/run.py "What is 17 * 23?"
```

This example uses the SDK exported from `pne`.

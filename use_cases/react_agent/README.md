# ReACT Agent Example

Run from the repository root:

```bash
uv run python use_cases/react_agent/run.py "What is 17 * 23?"
```

The script auto-detects the available backend in this order: OpenAI, Anthropic, Claude Code, Codex.
